{{page-title "Ex2"}}

<h1 class="align-content-end card-header">Services</h1>

<p>Choisissez les éléments à inclure</p>
<ul>
  {{#each @model.services as |srv|}}
    <li {{action "toggleService" srv.name}}>{{srv.name}}</li>
  {{/each}}
</ul>
<p class="card fw-bold">{{ this.serviceActifs }} {{plural @model.countActive "service"}} {{plural @model.countActive "selectionné"}}</p>
<p class="card fw-bold">Coût total : {{format-currency this.prix "€"}}</p>
{{outlet}}
