{{page-title "Sections"}}
<div style="display: flex; flex-flow: row nowrap">
  <div style="display: flex; flex-flow: column nowrap; width: 50%;">
    {{#each @model as |sec|}}
      <Section-ui @name={{sec.name}} @description={{sec.description}} @id={{sec.id}}></Section-ui>
    {{/each}}
  </div>

  <div>
    <a class="ui button">AJOUTER UNE SECTION</a>
    <a class="ui button">AJOUTER UN PRODUIT</a>
  </div>
</div>

{{outlet}}
